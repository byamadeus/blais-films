# Posters

Drop your poster images here. They're referenced in `src/data/films.js` as `/posters/<filename>`.

Current expected filenames (match what's in films.js):
- `clocked-out-streaming.png`
- `poster-kissedurmom-streaming.png`

Your source files are in `/poster-assets/` — just copy them here.
